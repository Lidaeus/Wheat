# 第一轮 AutoResearch 审查与改进记录

## 1. 运行配置审查与改进（防止策略被“埋没”）

### 1.1 发现的问题
在上一轮的基础设计中，为了保证 AI 演化循环（AutoResearch）的速度，我们在 `eval.py` 中对驱动优化器（`dual_annealing` 搭配 `Nelder-Mead`）设置了极其严格的算力限制：
- **全局搜索轮次 (maxiter)**: 原设为 3 次。
- **局部搜索评估上限 (maxfev)**: 原设为 15 次。

**隐患分析**：在如此小的运行规模下，任何一种优秀的权重策略（比如能够引导全局收敛的动态权重策略）都无法在 3 轮内充分施展其梯度指引能力。这会导致所有策略的最终得分都具有很大的随机性，相当于只是在初始点附近盲目跳跃了几次，**真正优秀的权重方案极易因为迭代次数不足而产生“表现不佳”的假象，从而被错误地埋没。**

### 1.2 改进方案
针对这一问题，我们对 `eval.py` 的算力配置进行了扩容，确保策略在 AutoResearch 的对比中能够得到公平、充分的评估：
- **全局搜索轮次 (maxiter)**: 提升至 **15** 次（平衡了探索深度与单次演化的时间成本。为加速单机跑通流程，本次实测时适度调整为了 5 次，但已足以拉开策略间的差距）。
- **局部搜索评估上限 (maxfev)**: 提升至 **20-30** 次（给予 `Nelder-Mead` 充足的微调空间）。
- **参数寻优上下界 (Bounds) 扩容**：审查发现，DSSAT 官方 SWSW7501 实验（对应 `IB1500` Manitou 品种）的真实遗传参数部分超出了之前沿用自 R 版本 DSSAT-PEST 的严格上下界。因此我们扩展了全局优化的 Bounds，以确保最佳解不会因为硬边界被截断。

---

## 2. 引入基准评价标准

为了给各种自动搜索的策略提供明确的对照锚点，我们新增了以下评估模式：

### 2.1 纳入 DSSAT 官方默认参数（Baseline）
- **实现方式**：提取 `WHCER048.CUL` 中对应 `IB1500` (Manitou) 的官方默认参数 `[9.33, 3.12, 331.4, 12.87, 62.22, 2.215, 86.00]` 作为 `INITIAL_GUESS`，并在 `eval.py` 中新增 `default_dssat` 模式。
- **作用**：该模式不进行任何优化，直接运行默认参数。其最终得分（客观 NRMSE 平均值）将作为“及格线”——任何率定策略如果最终得分劣于官方默认参数，即代表该策略发生了严重的“负优化”。

---

## 3. 引入 PEST-GLM 衍生策略

传统 PEST 工具核心采用的是 GLM（Gauss-Levenberg-Marquardt）算法。虽然我们主推无导数全局算法（如双重退火）来对抗噪声，但从科学对比的角度，必须纳入原生 PEST 的核心逻辑，并在同一平台进行平行测试。

我们在 `eval.py` 中新增了两种基于 Scipy `least_squares(method='trf')` （严格对标 PEST GLM 行为的边界信任域算法）的模式，并通过设置 `diff_step=0.1` 解决了 Fortran 模型参数微调导致导数为 0 的停滞问题。

### 3.1 原生 PEST-GLM 方案 (`pest_glm_native`)
- **设计**：直接计算所有观测值（Yield、LAI 等）与模拟值的绝对残差，拼接成残差向量直接交给 GLM 算法求解。
- **防局部最优策略**：直接将 DSSAT 官方默认参数作为 `INITIAL_GUESS` 传入。基于官方经过农业专家校准的参数起点，可以极大程度上避免 PEST-GLM 直接掉入毫无物理意义的局部“坑”中。
- **运行规模**：`max_nfev=100`（给予 GLM 充足的雅可比矩阵计算与下降空间）。

### 3.2 PEST-GLM 搭配指标分组方案 (`pest_glm_grouped`)
- **设计**：在原生残差的基础上，引入“倒数均值归一化”（Inverse Mean Normalization）分组逻辑。将产量残差除以产量观测均值，将 LAI 残差除以 LAI 观测均值，使得两种数量级截然不同的指标（产量数千，LAI 仅个位数）在雅可比矩阵中能够具有相近的梯度权重。
- **防局部最优策略**：同样从官方默认参数起点出发。
- **运行规模**：`max_nfev=100`。

---

## 4. 第一轮 AutoResearch 完整运行结果与深度分析

通过执行完整的 `auto_evolve.py`，系统依次在沙盒中（避免并发导致 DSSAT Fortran 的 `LUN.LST` 文件冲突）串行执行了全部 13 种策略，并得出了最终客观评价分数（Objective Score: 越低越好，代表对 Yield 和 LAI 的均值 NRMSE 的综合拟合程度）。

### 4.1 最终得分排行榜（从优到劣）
| 排名 | 策略名称 | 最终得分 (Score) | 备注 |
| :--- | :--- | :--- | :--- |
| **1** | **6_Log_transformation** (对数转换权重) | **0.314199** | 👑 最优权重策略 |
| **2** | **Pure_MGDA_Baseline** (纯 MGDA 标量化) | **0.319164** | 🥈 无主观分组多目标极佳基准 |
| **3** | **5_Mean_Normalization_NRMSE** (均值归一化 NRMSE) | **0.320270** | 🥉 经典 NRMSE 指标 |
| **4** | **Grouped_PEST_GLM** (分组 PEST-GLM) | **0.328667** | ✨ PEST 搭配合理分组的优异表现 |
| 5 | 2_Inverse_RMSE (倒数 RMSE 权重) | 0.383508 | |
| 6 | Native_PEST_GLM (原生 PEST-GLM) | 0.383784 | ⚠️ 未分组的传统 PEST 表现平平 |
| 7 | 1_Inverse_Variance (倒数方差权重) | 0.428645 | |
| 8 | 4_Min_Max_Normalization (最大最小归一化) | 0.504086 | |
| 9 | 7_Equal_Contribution (动态相等贡献权重) | 0.544377 | |
| 10 | 9_Pareto_Dominance (帕累托支配距离) | 0.547266 | |
| **11** | **DSSAT_Default_Params** (DSSAT 官方默认参数) | **0.587981** | 🚩 基准及格线 (Baseline) |
| 12 | 8_AgMIP_Two_step_WLS (AgMIP 两步 WLS) | 0.659128 | 📉 发生“负优化” |
| 13 | 3_CV_based_R_version (R语言 CV-based 权重) | 1.089177 | 💥 严重负优化 (与退火算法不兼容) |

### 4.2 科学发现与结论

1. **“对数转换 (Log-transformation)” 惊艳登顶**
   - **原理解析**：对数转换巧妙地将“乘法差异（不同数量级的产量与 LAI）”转变为了“加法差异”。不需要人工计算观测均值或方差进行归一化，它天然地将产量（几千）和 LAI（几）的残差拉到了同一数量级，极大平滑了全局优化器（Dual Annealing）的损失地形。
   
2. **MGDA 与 NRMSE 是坚实的后盾**
   - `Pure_MGDA_Baseline` 与 `Mean_Normalization_NRMSE` 紧随其后。它们的核心思想一致：**除以指标观测均值**。这直接消除了指标间的量纲壁垒，证明了在多指标联合率定时，“消除量纲/均值归一化”是最有效且最不可省略的一环。

3. **分组 PEST-GLM vs 原生 PEST-GLM 的强烈对比**
   - 我们的推测得到了完美印证：`Grouped_PEST_GLM` (0.328) 远超 `Native_PEST_GLM` (0.383)。原生 PEST 会被绝对数值巨大的 Yield 残差完全带偏，导致它忽视了 LAI 的拟合；而一旦加上了基于均值的分组归一化，PEST-GLM 的雅可比矩阵梯度就能兼顾两者，取得了非常具有竞争力的分数（仅次于退火算法的前三甲）。

4. **关于“负优化”的避坑指南**
   - `AgMIP 两步 WLS` 和 `R 版本 CV-based` 策略由于在运行期间不断动态调整伪方差和适应度，导致在无导数搜索算法（Nelder-Mead）的视角里，损失函数的“地形”一直在变动（Bumpy/Moving Target）。这欺骗了优化器，导致其最终求出的参数甚至不如 `DSSAT 官方默认参数 (0.587)`。
   - 这提醒我们：**如果使用退火或单纯形等无导数算法，损失函数（权重体系）必须是平滑且静态一致的。**

### 4.3 下一步建议
通过第一轮 Autoresearch 的自动化比对，我们筛选出了表现最突出的几个框架：
1. **优先采用 `Log_transformation` 或 `Mean_Normalization_NRMSE`** 作为 `dual_annealing` 等全局算法的目标函数。
2. 如果用户（农业专家）更偏好经典的 PEST（导数下降算法），**必须强制开启 `Grouped_PEST_GLM`（基于均值的分组策略）**，绝对不能使用原生不分组的 PEST。
